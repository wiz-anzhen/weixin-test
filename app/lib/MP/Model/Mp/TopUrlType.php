<?php
//Don't edit this file which is generated by Bluefin Lance.
namespace MP\Model\Mp;

use Bluefin\Convention;
use Bluefin\Data\ValidatorInterface;

class TopUrlType implements ValidatorInterface
{
    const NONE = 'none';
    const BACKGROUND = 'background';
    const TOP = 'top';
    const TOP_CAROUSEL = 'top_carousel';
    const CENTER_CAROUSEL_ONE = 'center_carousel_one';
    const CENTER_CAROUSEL = 'center_carousel';
    const PROCUREMENT = 'procurement';

    private static $_data;

    public static function getDictionary()
    {
        if (!isset(self::$_data))
        {
            self::$_data = array(
                self::NONE => _META_('mp.top_url_type.none'),
                self::BACKGROUND => _META_('mp.top_url_type.background'),
                self::TOP => _META_('mp.top_url_type.top'),
                self::TOP_CAROUSEL => _META_('mp.top_url_type.top_carousel'),
                self::CENTER_CAROUSEL_ONE => _META_('mp.top_url_type.center_carousel_one'),
                self::CENTER_CAROUSEL => _META_('mp.top_url_type.center_carousel'),
                self::PROCUREMENT => _META_('mp.top_url_type.procurement'),
            );
        }

        return self::$_data;
    }

    public static function getValues()
    {
        $data = self::getDictionary();
        return array_keys($data);
    }

    public static function getDisplayName($value)
    {
        $data = self::getDictionary();
        return $data[$value];
    }

    public static function getDefaultValue()
    {
        return self::NONE;
    }

    public function validate($value)
    {
        $data = self::getDictionary();
        return array_key_exists($value, $data);
    }
}